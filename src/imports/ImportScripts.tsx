// ImportScripts.tsx

import {
  curDate, curTime, curFormat, curDay,
} from "@scripts/date";

// -------------------------------------------------------------------------------------------------
export {
  curDate,
  curTime,
  curFormat,
  curDay,
};