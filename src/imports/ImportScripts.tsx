// ImportScripts.tsx

import {
  curDate, curTime, curFormat, curDay, curCurrency, isKorean,
} from "@scripts/timeZone";

// -------------------------------------------------------------------------------------------------
export {
  curDate,
  curTime,
  curFormat,
  curDay,
  curCurrency,
  isKorean,
};